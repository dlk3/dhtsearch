SSL certificate files named "fullchain.pem" and "privkey.pem" are expected here
but are not included for security reasons.  Put your own files here with those
names or reconfigure ../conf/virtualhosts.conf and/or the volume mapping in 
../../docker-compose.yaml to suit your needs.
